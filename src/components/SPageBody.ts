/**
 * <s-page-body>
 *
 * The body slot inside an <s-page>. Authors style this with padding,
 * max-width, etc. without breaking page chrome. Created automatically
 * by Stapler if an <s-page> contains raw children with no <s-page-body>.
 */
export class SPageBody extends HTMLElement {
  static readonly TAG = 's-page-body'
}
